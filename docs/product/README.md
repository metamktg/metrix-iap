# docs/product — product loop

Product-level documentation extracted from the master blueprint so it can be referenced without
loading the full backend architecture document.

| Document | Source | Purpose |
| :---- | :---- | :---- |
| [`METRIX_Product_Loop.md`](METRIX_Product_Loop.md) | Blueprint v2.0 §4 | The nine-stage product loop: ONBOARD → INPUT → STORE → VALIDATE → ANALYZE → OUTPUT → REVIEW → APPROVE → REMEMBER. |

The blueprint stays canonical. If §4 changes there, this file is updated to match — never the
other way around.

## Related

- [`../architecture/METRIX_IAP_MASTER_BLUEPRINT_v2.0.md`](../architecture/METRIX_IAP_MASTER_BLUEPRINT_v2.0.md) — canonical source
- [`../data-model/`](../data-model/) — cohort architecture and the Listen Layer metric contract
- [`../security/`](../security/) — tenancy enforcement for the loop's write paths
