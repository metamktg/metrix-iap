Review the current live Replit implementation and perform a surgical UI/UX refinement pass on the existing Metrix IAP app.

Do not reference legacy HTML.
Do not rebuild the app.
Do not replace the routing system.
Do not change data-binding logic unless required to correct visible UI/data mismatches.
Do not introduce fake metrics, fake screenshots, or demo content into production views.

Use the current deployment state and attached screenshots as the visual QA reference.

Current visible state:
- The six primary nav layers are now present:
  01 Listen
  02 Analysis
  03 Strategy
  04 Report Builder
  05 MST
  06 Metrix Agent
- The sidebar structure is closer, but still feels unfinished:
  - primary layer rows are too sparse
  - chevrons appear on items that may not expose children
  - child navigation is not visibly resolved in the screenshots
  - spacing and hierarchy need polish
- The dashboard cards are functional but visually heavy and slightly generic.
- The workspace switcher is usable but needs tighter alignment, cleaner selected state, and better status-dot consistency.
- The global shell needs a more premium, cohesive SaaS feel across all six layers.

Primary objective:
Refine the current UI into a professional, polished Metrix command-center interface while preserving the previously established six-layer architecture, routes, and data wiring.

1. Navigation hierarchy and sidebar polish

Maintain this exact primary structure:

01 Listen
- Alerts
- Signals
- Suggestions

02 Analysis
- Overview
- Concept Library
- Creative Map
- Audience
- Placements

03 Strategy
- Overview
- Strategy Map
- Avatars / ICP
- Hypothesis Queue
- Brief Builder

04 Report Builder

05 MST

06 Metrix Agent

Sidebar requirements:
- Keep the numbered primary layers.
- Use compact, premium spacing.
- Use subtle numeric labels: 01, 02, 03, etc.
- Parent rows should feel like system layers, not generic menu items.
- Child rows should be clearly nested, smaller, and slightly indented.
- Do not duplicate parent labels.
- Do not show a chevron on Report Builder, MST, or Metrix Agent unless those sections actually expose nested child pages.
- If a parent has child pages, the chevron should align perfectly and rotate smoothly when expanded.
- Active state must be obvious but not loud:
  - left blue rail
  - subtle blue-tinted background
  - brighter label
  - optional active child dot
- Only one nav item should appear active at a time.
- Parent can show expanded state, but child active state must be primary.
- Keep mobile/collapsed behavior stable.

Suggested visual treatment:
- Sidebar width: keep current width unless layout forces change.
- Parent item height: consistent 36-40px.
- Child item height: consistent 30-34px.
- Section gap between primary groups: 8-12px.
- Use one visual rhythm throughout, not mixed paddings.
- Avoid oversized icons.
- Use line icons only.
- No emoji icons.

2. Topbar and breadcrumb refinement

The topbar should clearly show:
- workspace/agency breadcrumb
- current page title
- optional Watch state
- notification/account controls

Fix:
- breadcrumb spacing
- tiny/low-contrast labels
- inconsistent topbar alignment
- over-wide blank zones where possible
- awkward title positioning on large screens

Breadcrumb examples:
- MMA / Bookster / Listen / Signals
- MMA / Bookster / Analysis / Creative Map
- MMA / Bookster / Strategy / Brief Builder

Do not show legacy loop labels or duplicate stage text.

3. Workspace switcher polish

Refine the workspace switcher visible in the screenshots.

Requirements:
- Keep current grouping:
  - Master Command Center
  - Agency
  - Brand
  - Client
  - Internal
- Improve selected state for Bookster:
  - selected background should be polished, not overly saturated
  - selected checkmark aligned to right
  - yellow status dot aligned consistently
- Status dots should use consistent sizing and color:
  - green = healthy/connected
  - yellow = watch/attention
  - orange/red = issue
- Improve vertical rhythm inside the dropdown:
  - consistent row heights
  - consistent icon container sizes
  - consistent label/subtitle spacing
- Prevent dropdown from visually colliding with the main content.
- Maintain keyboard accessibility and click-outside behavior.

4. Dashboard/card visual refinement

Apply a cohesive card system across all six primary layers.

Cards should use:
- consistent radius
- consistent border opacity
- consistent internal padding
- consistent title/meta hierarchy
- consistent badge styling
- cleaner metric alignment
- improved empty states

Current dashboard card issues to fix:
- large KPI cards feel slightly flat and generic
- active workspace card is too visually dense
- small metadata text lacks hierarchy
- some rows feel cramped while outer sections feel overly spaced
- badge styling varies too much

Standardize:
- Card background: deep navy panel with subtle blue tint.
- Border: thin blue-gray with low opacity.
- Hover: slight border lift and brightness, no dramatic movement.
- Metric values: clear, high contrast, consistent size.
- Metric labels: muted uppercase with letter spacing.
- Subtext: muted, readable, not too small.
- Use the same badge styles everywhere.

5. Six-layer page consistency

Audit all six primary areas:

Listen
- Alerts
- Signals
- Suggestions

Analysis
- Overview
- Concept Library
- Creative Map
- Audience
- Placements

Strategy
- Overview
- Strategy Map
- Avatars / ICP
- Hypothesis Queue
- Brief Builder

Report Builder

MST

Metrix Agent

Each page should have:
- consistent page header
- consistent breadcrumb
- consistent card spacing
- consistent empty state
- consistent DataSourceBadge placement where applicable
- consistent loading state
- consistent error state

Do not let one page look like a prototype while another looks finished.

6. Data integrity and source mapping

Preserve all existing data-binding logic.

Specifically verify:
- ReviewQueue remains mapped to Listen / Alerts.
- IntelligenceCards remain mapped to Listen / Signals.
- BSIL Suggestions remain mapped to Listen / Suggestions.
- BSIL Suggestions remain campaign/ad-set scope only.
- No creative-level BSIL budget suggestions.
- Existing badge counts remain data-backed.
- If count data is unavailable, hide badge rather than showing fake numbers.
- No hardcoded fake counts.
- No demo fallback in production views.
- DataSourceBadge remains visible and collapsible in both production and development.
- Do not reintroduce `import.meta.env.PROD` hiding logic.

7. User-facing content and asset rendering

Improve missing/placeholder content without inventing data.

Rules:
- Use existing repository/workspace asset records if available.
- If Concept Library has asset records, render clean asset preview cards.
- If Brief Builder has approved briefs, render clean brief preview cards.
- If no live production data exists, show:
  - “No production data connected yet”
  or
  - “Pending Stage 2 implementation”
- Do not use app UI screenshots as product assets.
- Treat attached screenshots as QA references for visual state only.
- Do not embed screenshots into Concept Library or Brief Builder unless the repository already identifies them as client creative assets.
- If an image/media asset exists but is missing metadata, render it with a muted “metadata pending” state.

Concept Library asset cards should include, if available:
- asset thumbnail
- concept name
- concept code
- variable stack chips
- performance classification
- source badge
- last updated timestamp

Brief Builder cards should include, if available:
- brief title
- status
- target ICP
- concept/angle
- variable stack
- source insight
- approval state

8. Empty, loading, and error states

Create one reusable page state pattern.

Empty state:
- title
- short explanation
- source/status badge
- optional next action
- no fake metrics

Examples:
- “No production data connected yet”
- “Pending Stage 2 implementation”
- “Connect a source or run an analysis to populate this layer”

Loading state:
- use subtle skeletons, not spinners everywhere
- preserve layout height to prevent jumping

Error state:
- clear message
- retry action if available
- source/debug detail collapsed by default

9. Styling and Tailwind/CSS refinement

Perform a targeted styling pass only.

Areas to adjust:
- sidebar nav item spacing
- nested subnav indentation
- topbar height and alignment
- workspace dropdown width, padding, and z-index
- card padding and border consistency
- badge color system
- metric typography scale
- hover/focus states
- page gutters and max-width behavior
- responsive behavior

Do not:
- create a new design system
- replace all components
- change the theme away from Metrix dark navy/blue
- introduce bright generic gradients
- add unnecessary animations
- add visual noise

Preferred aesthetic:
- dark premium SaaS
- sharp
- compact
- confident
- operator-console feel
- blue active states
- restrained status colors
- clear hierarchy

10. QA requirements

After changes, test these routes manually:

/app/listen/alerts
/app/listen/signals
/app/listen/suggestions
/app/analysis
/app/analysis/concept-library
/app/analysis/creative-map
/app/analysis/audience
/app/analysis/placements
/app/strategy
/app/strategy/map
/app/strategy/avatars
/app/strategy/hypothesis-queue
/app/strategy/brief-builder
/app/reports
/app/mst
/app/agent

Also test workspace-scoped equivalents if the app requires workspace IDs.

Verify:
- active sidebar state
- expanded parent state
- breadcrumbs
- badges
- DataSourceBadge
- empty states
- mobile/collapsed sidebar
- workspace switcher
- no overlapping labels
- no broken links
- no fake metrics

11. Implementation report

After completion, provide a concise report with:

- Files changed
- CSS/Tailwind classes adjusted
- Components refined
- Assets integrated, if any
- Pages with new placeholder states
- Final nav mapping
- Badge data source per nav item
- Confirmation that ReviewQueue, IntelligenceCards, BSIL Suggestions, and DataSourceBadge wiring were not broken
- Confirmation that BSIL remains campaign/ad-set scope only
- Build/typecheck/lint results
- Known limitations

Acceptance criteria:
- The interface looks polished and intentional, not prototype-like.
- Six-layer nav remains exact.
- Child nav is cleanly nested.
- No duplicate labels.
- No visual overlap.
- Workspace switcher feels premium.
- Cards, badges, headers, and empty states are consistent across layers.
- Data-binding and routing logic remain stable.
- No fake production data is introduced.