Before I accept these changes, perform a final surgical audit and fix the remaining UI/data integrity issues visible in the screen recording.

Do not rewrite the architecture. Do not replace the current UI system. Do not introduce new mock data. Keep the existing Supabase/event-table wiring and only make targeted fixes.

Context:
- Metrix IAP has an IAP-ISS flow: Analysis -> Reporting -> Strategy -> Brief Generation.
- MST is optional and activates after briefs for validation, matrix testing, creative DNA, and optimization.
- Creative Library must respect MST logic: C1-C4 are distinct concept families/columns, while C1A, C1B, etc. are angles/variants inside that concept.
- BSIL Suggestions must remain budget-object only. It should not contain creative-level recommendations.
- Review Queue and Intelligence Cards must remain sourced from event tables.
- DataSourceBadge must remain visible/collapsible in production and development.

Fix these specific issues:

1. Sidebar stage label duplication/overlap
- The left navigation currently shows duplicated/overlapping stage labels such as:
  - ONBOARD & INPUT plus ONBOARD -> INPUT
  - REVIEW & APPROVE plus REVIEW -> APPROVE
  - REMEMBER duplicated
- Normalize the sidebar section headers so only one clean section label appears per group.
- Preserve the grouped structure, but remove visual duplication, overlap, and redundant arrow labels.
- Verify this at dashboard, BSIL Suggestions, Briefs, Creative Library, Settings, and run detail pages.

2. BSIL Suggestions status/count mismatch
- The BSIL Suggestions page header says “2 pending,” but the visible list includes one approved row.
- Fix the logic one of two ways:
  A. If this page is meant to be pending-only, filter out approved rows.
  B. If this page is meant to show all suggestions, rename the header to “All Suggestions” and show a secondary count like “2 pending.”
- Do not let a pending-only header show approved rows.
- Keep BSIL budget-object only at campaign/ad_set scope.

3. Creative Library concept duplication
- Creative Library currently shows both C2 and C4 as “Time-poor learner product demo / 1,000 books proof.”
- C1-C4 should not present as duplicate concept families unless the data explicitly marks one as an iteration/extension of the other.
- Fix by either:
  A. Mapping C4 to its correct distinct concept name from the underlying data, or
  B. If C4 is truly a continuation of C2, display it as an extension/iteration of C2 instead of a standalone duplicate concept.
- Keep the C1/C2/C3/C4 distinction aligned with MST methodology: concept family first, angle/variant second, iteration/relaunch third.

4. Cohort Settings label polish
- Remove duplicated display text like “Device device.”
- Show the human label as the primary text: Device, Creative Angle, Age x Gender, Placement.
- If the raw key is useful, show it as a small muted code chip, for example: device, creative_angle, age_gender, placement.
- Do not show the same label twice in normal body text.

5. Source badge verification
- Confirm DataSourceBadge and DataSourceBadgeToggle are visible/collapsible in production and development.
- Verify collapsed-by-default behavior on all pages that use data source badges.
- Do not restore any `import.meta.env.PROD` hiding logic.
- Add/adjust tests if current tests only cover one route.

6. Event-table wiring verification
- Confirm Review Queue counts, Intelligence Card counts, BSIL counts, and Dashboard summary counts come from the intended event/source tables, not static arrays.
- Remove any remaining hardcoded demo fallbacks except explicit sample/demo seed data.
- If a fallback exists, it must be clearly scoped to sample/demo mode and not production.

7. Final validation
- Run typecheck/lint/build or the closest available project validation.
- Provide a concise report with:
  - Files changed
  - What was fixed
  - What data source each affected page now reads from
  - Any known remaining limitations
  - Confirmation that no unrelated UI architecture was rewritten

Acceptance criteria:
- Sidebar labels no longer duplicate or overlap.
- BSIL page count/status is internally consistent.
- Creative Library no longer presents duplicate C2/C4 concept families unless explicitly labeled as an extension/iteration.
- Cohort labels are clean and readable.
- Source badges are visible/collapsible in prod and dev.
- ReviewQueue and IntelligenceCards remain event-table sourced.
- Validation passes.